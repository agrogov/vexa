{{- if and .Values.agentApi.enabled (eq (default "process" .Values.agentApi.orchestrator) "kubernetes") }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vexa.componentName" (list . "agent-api") }}
  labels:
    {{- include "vexa.labels" . | nindent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "vexa.componentName" (list . "agent-api") }}
  labels:
    {{- include "vexa.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["pods/exec"]
    verbs: ["create", "get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "vexa.componentName" (list . "agent-api") }}
  labels:
    {{- include "vexa.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "vexa.componentName" (list . "agent-api") }}
subjects:
  - kind: ServiceAccount
    name: {{ include "vexa.componentName" (list . "agent-api") }}
    namespace: {{ .Release.Namespace }}
{{- end }}
