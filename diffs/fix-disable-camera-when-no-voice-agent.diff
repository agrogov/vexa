diff --git a/services/vexa-bot/core/src/index.ts b/services/vexa-bot/core/src/index.ts
index b7bf31d..3248911 100644
--- a/services/vexa-bot/core/src/index.ts
+++ b/services/vexa-bot/core/src/index.ts
@@ -1153,11 +1153,15 @@ export async function runBot(botConfig: BotConfig): Promise<void> {// Store botC
     Object.defineProperty(window, "outerHeight", { get: () => 1080 });
   });
 
-  // Always initialize virtual camera and avatar display
-  try {
-    await initVirtualCamera(botConfig, page);
-  } catch (err: any) {
-    log(`[Bot] Virtual camera initialization failed (non-fatal): ${err.message}`);
+  // Initialize virtual camera and avatar display only when voice agent is enabled
+  if (botConfig.voiceAgentEnabled) {
+    try {
+      await initVirtualCamera(botConfig, page);
+    } catch (err: any) {
+      log(`[Bot] Virtual camera initialization failed (non-fatal): ${err.message}`);
+    }
+  } else {
+    log("[Bot] Voice agent disabled — skipping virtual camera initialization");
   }
 
   // Always initialize chat service so chat read/write works for every bot
