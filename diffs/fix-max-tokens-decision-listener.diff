diff --git a/services/decision-listener/enrichment.py b/services/decision-listener/enrichment.py
index 3973544..f8ba6ce 100644
--- a/services/decision-listener/enrichment.py
+++ b/services/decision-listener/enrichment.py
@@ -94,8 +94,7 @@ async def _web_search(query: str) -> str:
                 },
                 {"role": "user", "content": query},
             ],
-            temperature=0.2,
-            max_tokens=512,
+            max_completion_tokens=512,
         )
         return response.choices[0].message.content.strip()
     except Exception as e:
@@ -176,8 +175,7 @@ async def _enrich_entity(
                     },
                 ],
                 response_format={"type": "json_object"},
-                temperature=0.1,
-                max_tokens=512,
+                max_completion_tokens=512,
             )
             synthesis = json.loads(response.choices[0].message.content)
         except Exception as e:
diff --git a/services/decision-listener/llm.py b/services/decision-listener/llm.py
index 006a07e..37ef866 100644
--- a/services/decision-listener/llm.py
+++ b/services/decision-listener/llm.py
@@ -69,8 +69,7 @@ async def analyze_window(segments: List[dict]) -> Optional[dict]:
             ],
             tools=[tool_schema],
             tool_choice={"type": "function", "function": {"name": "capture_meeting_item"}},
-            temperature=0.1,
-            max_tokens=256,
+            max_completion_tokens=256,
         )
 
         msg = response.choices[0].message
@@ -132,8 +131,7 @@ async def is_duplicate_llm(new_summary: str, existing_summaries: List[str]) -> b
                 {"role": "system", "content": system},
                 {"role": "user", "content": user},
             ],
-            temperature=0,
-            max_tokens=5,
+            max_completion_tokens=5,
         )
         answer = response.choices[0].message.content.strip().upper()
         logger.debug(f"Dedup LLM answer: {answer!r} for: {new_summary[:60]}")
diff --git a/services/decision-listener/narrative.py b/services/decision-listener/narrative.py
index db1ee56..f36a208 100644
--- a/services/decision-listener/narrative.py
+++ b/services/decision-listener/narrative.py
@@ -139,8 +139,7 @@ async def generate_summary(
                 {"role": "user", "content": user_message},
             ],
             response_format={"type": "json_object"},
-            temperature=0.2,
-            max_tokens=256,
+            max_completion_tokens=256,
         )
 
         content = response.choices[0].message.content
@@ -207,8 +206,7 @@ async def generate_narrative(
                 {"role": "user", "content": user_message},
             ],
             response_format={"type": "json_object"},
-            temperature=0.3,
-            max_tokens=4096,
+            max_completion_tokens=4096,
         )
 
         content = response.choices[0].message.content
