diff --git a/helm/charts/vexa/Chart.yaml b/helm/charts/vexa/Chart.yaml
index a721f108..fbb1da03 100644
--- a/helm/charts/vexa/Chart.yaml
+++ b/helm/charts/vexa/Chart.yaml
@@ -3,7 +3,7 @@ name: vexa
 description: Vexa - self-hosted real-time meeting transcription platform
 type: application
 version: 0.1.0
-appVersion: "0.6.0"
+appVersion: "0.6.1"
 home: https://github.com/Vexa-ai/vexa
 sources:
   - https://github.com/Vexa-ai/vexa
diff --git a/helm/charts/vexa/templates/deployment-bot-manager.yaml b/helm/charts/vexa/templates/deployment-bot-manager.yaml
index 080d835d..9d918830 100644
--- a/helm/charts/vexa/templates/deployment-bot-manager.yaml
+++ b/helm/charts/vexa/templates/deployment-bot-manager.yaml
@@ -68,6 +68,10 @@ spec:
             {{- end }}
             - name: DOCKER_NETWORK
               value: {{ .Values.botManager.bot.dockerNetwork | quote }}
+            {{- if .Values.botManager.bot.nodeSelector }}
+            - name: BOT_NODE_SELECTOR
+              value: {{ .Values.botManager.bot.nodeSelector | quote }}
+            {{- end }}
             - name: BOT_CALLBACK_BASE_URL
               value: {{ default (printf "http://%s.%s.svc.%s:%d" (include "vexa.componentName" (list . "bot-manager")) .Release.Namespace .Values.global.clusterDomain (.Values.botManager.service.port | int)) .Values.botManager.callbackBaseUrl | quote }}
             - name: BOT_MANAGER_CALLBACK_URL
@@ -81,21 +85,21 @@ spec:
             {{- if .Values.botManager.zoom.clientId }}
             - name: ZOOM_CLIENT_ID
               value: {{ .Values.botManager.zoom.clientId | quote }}
-            {{- else if or .Values.botManager.zoom.existingSecretName .Values.secrets.existingSecretName }}
+            {{- else if .Values.botManager.zoom.existingSecretName }}
             - name: ZOOM_CLIENT_ID
               valueFrom:
                 secretKeyRef:
-                  name: {{ default .Values.secrets.existingSecretName .Values.botManager.zoom.existingSecretName | quote }}
+                  name: {{ .Values.botManager.zoom.existingSecretName | quote }}
                   key: {{ .Values.botManager.zoom.clientIdKey | quote }}
             {{- end }}
             {{- if .Values.botManager.zoom.clientSecret }}
             - name: ZOOM_CLIENT_SECRET
               value: {{ .Values.botManager.zoom.clientSecret | quote }}
-            {{- else if or .Values.botManager.zoom.existingSecretName .Values.secrets.existingSecretName }}
+            {{- else if .Values.botManager.zoom.existingSecretName }}
             - name: ZOOM_CLIENT_SECRET
               valueFrom:
                 secretKeyRef:
-                  name: {{ default .Values.secrets.existingSecretName .Values.botManager.zoom.existingSecretName | quote }}
+                  name: {{ .Values.botManager.zoom.existingSecretName | quote }}
                   key: {{ .Values.botManager.zoom.clientSecretKey | quote }}
             {{- end }}
             - name: ADMIN_TOKEN
diff --git a/helm/charts/vexa/templates/deployment-dashboard.yaml b/helm/charts/vexa/templates/deployment-dashboard.yaml
index 4dda888b..b0115950 100644
--- a/helm/charts/vexa/templates/deployment-dashboard.yaml
+++ b/helm/charts/vexa/templates/deployment-dashboard.yaml
@@ -63,7 +63,7 @@ spec:
             {{- end }}
             {{- end }}
             {{- if .Values.decisionListener.enabled }}
-            - name: NEXT_PUBLIC_DECISION_LISTENER_URL
+            - name: DECISION_LISTENER_URL
               value: {{ default "http://localhost:8765" .Values.decisionListener.publicUrl | quote }}
             {{- end }}
           resources:
diff --git a/helm/charts/vexa/values.yaml b/helm/charts/vexa/values.yaml
index 17aacf6a..9da7893b 100644
--- a/helm/charts/vexa/values.yaml
+++ b/helm/charts/vexa/values.yaml
@@ -125,6 +125,11 @@ botManager:
     # Image name Bot Manager will ask the orchestrator to run (used by docker/kubernetes orchestrators)
     imageName: "vexa-bot:dev"
 
+    # Node selector for bot Pods (kubernetes orchestrator only).
+    # JSON string, e.g. '{"k8s.infobip.com/nodepool":"worker"}'
+    # Empty means no nodeSelector — scheduler decides.
+    nodeSelector: ""
+
     # Only relevant for Docker orchestrator; set if your bot containers must join a specific Docker network.
     dockerNetwork: ""
     resources:
